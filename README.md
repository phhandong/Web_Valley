# 溪谷小农场

一个使用 Vite、TypeScript 和 Canvas 2D 制作的原创中文像素农场 Demo。

## 运行

```bash
npm install
npm run dev
```

打开终端显示的本地地址即可游玩。生产构建与规则测试：

```bash
npm run build
npm test
```

## 操作

- `WASD` 或方向键移动
- `1`–`4` 切换锄头、种子、水壶和收获
- `空格` 对面前的土地使用工具
- 鼠标点击相邻土地直接操作
- `E` 与小屋、杂货摊和出货箱互动

进度会自动保存在浏览器中，可从帮助面板重新开始。
